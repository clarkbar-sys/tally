# Confirmed false positives, one fingerprint per line. Do not add an entry
# without leaving a comment on the line above explaining why it's safe.
# Run locally: gitleaks detect --config .gitleaks.toml --redact

# design/tally-spike.html:341, original commit only -- a variable holding
# the design spike's localStorage namespace string, not a credential.
# It was renamed in a later commit to stop matching this rule going
# forward, but the original commit is already pushed/shared, so it
# stays in history and the finding still needs suppressing there.
5369102fa46fbab654c3f3a9094771cb93f51dc5:design/tally-spike.html:generic-api-key:341
